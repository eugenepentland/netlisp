"""EDA Sync plugin for KiCad 10 (IPC API).

The action entry point lives in `eda_sync_action.run()`. Tests can import the
sub-modules directly:
    plugins.netlist_parser, plugins.eda_client, plugins.oauth_client,
    plugins.config, plugins.sync_core
"""
