# guardian-snapshot v1
src/bench_layout.zig: const BENCH_DONE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bench_layout.zig: const DEFAULT_REPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bench_layout.zig: const NS_PER_MS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_10 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_11 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_12 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_13 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_14 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_15 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_5 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_7 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_BYTE_9 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_VARIANT_BYTE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/bom.zig: const UUID_VERSION_BYTE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const BUILD_ERROR_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const BUILD_FAILED_ASSERTION_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const CANNOT_WRITE_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const DIAG_ERROR_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const FAIL_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const IDENTITY_RESOLUTION_ERROR_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const OUTPUT_DIR_FLAG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const OUT_OF_MEMORY_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const PASS_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const PROJECT_DIR_FLAG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const WARN_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/commands.zig: const WROTE_BYTES_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/config.zig: const DOTENV_PATH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/config.zig: const MAX_DOTENV_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/config.zig: const WHITESPACE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const FAB_HEADER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const FULL_TURN_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const LAYER_FAB is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const LAYER_SILK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const ROT_135_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const ROT_225_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const ROT_315_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const ROT_45_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const SHAPE_ROUNDRECT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/footprint.zig: const SILK_HEADER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol.zig: const KICAD_SYMBOL_LIB_FORM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const DEFAULT_BODY_HALF_SIZE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const PIN_ANGLE_BOTTOM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const PIN_ANGLE_RIGHT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const PIN_ANGLE_TOP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/convert/symbol_helpers.zig: const UNCONNECTED_LABEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const DIST_BASE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const DIST_EXTRA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const LEN_BASE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const LEN_EXTRA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const MAX_CHAIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const MAX_DIST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/deflate.zig: const MAX_MATCH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/diagram.zig: const DIAGRAM_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/render.zig: const CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/types.zig: const CLASS_CLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/types.zig: const CLASS_CONTROL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/types.zig: const CLASS_GROUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/types.zig: const CLASS_POWER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/diagram/types.zig: const CLASS_RF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const DECOUPLE_BULK_FARADS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const GROUP_COVERAGE_MIN_BLOCKS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const GROUP_COVERAGE_NAME_LIMIT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const PERCENT_MULTIPLIER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const TEST_CAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/erc.zig: const VOLTAGE_MISMATCH_TOLERANCE_V is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/builders.zig: const ASSERT_RANGE_ARITY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/builders.zig: const MAX_BUS_PORT_EXPANSION is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/builtins.zig: const E96 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/builtins.zig: const ZERO_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/design_block.zig: const MAX_BUS_EXPANSION is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/design_block.zig: const TEST_CAP_FAMILY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/env.zig: const DECOUPLING_PER_PIN_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/env.zig: const PULLUP_RANGE_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/env.zig: const SERIES_ELEMENT_MAX_INDEX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/env.zig: const SERIES_ELEMENT_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const GIGA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const KILO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const MEGA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const MICRO_THRESHOLD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const MILLI_THRESHOLD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const NANO_THRESHOLD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const ONE_UNIT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const TERA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const WHOLE_NUMBER_LIMIT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/fmt.zig: const ZERO_UNIT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/ids.zig: const ID_FIRST_LETTER_RANGE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/ids.zig: const ID_GEN_MAX_ATTEMPTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/instance.zig: const SERIES_NAMED_REF_MIN_ARITY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/modules.zig: const FOOTPRINT_FORM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/modules.zig: const MAX_MODULE_DEPTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/modules.zig: const PASSIVES_PRELUDE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const CURRENT_CONVERGENCE_A is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const DEFAULT_DERATING is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const PERCENT_FRACTION_BASE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const PERCENT_FULL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const RATING_MIDPOINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const SENTINEL_CURRENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_budget.zig: const ZERO_VOLTAGE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/power_sequencing.zig: const SUB_PATH_BUF_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/rails.zig: const GND_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/rails.zig: const RATING_MIDPOINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/rails.zig: const SENTINEL_CAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/rails.zig: const SUB_PATH_BUF_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/suggest.zig: const LIB_PREFIXES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/suggest.zig: const MAX_EDIT_DISTANCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/suggest.zig: const MAX_NAME_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/validate.zig: const SECTION_DESCRIPTION_MAX_CHARS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/eval/validate.zig: const VOLTAGE_MISMATCH_TOLERANCE_V is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_fab.zig: const AUTO_OUTLINE_MARGIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_gerber.zig: const EDGE_W_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_gerber.zig: const PAD_ARC_SEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_gerber.zig: const SILK_W_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_gerber.zig: const TEXT_PX_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_gerber.zig: const THERMAL_SPOKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const FOOTPRINT_PATH_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const MODEL_MAX_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_10 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_11 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_12 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_13 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_14 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_15 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_5 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_7 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_BYTE_9 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_VARIANT_BYTE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad.zig: const UUID_VERSION_BYTE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const CUSTOM_PAD_ANCHOR_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const DEFAULT_ROUNDRECT_RRATIO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const FAB_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const KICAD_FILL_NONE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const KICAD_LAYER_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const KICAD_STROKE_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const PAD_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const POLY_MIN_POINTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const RECT_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const SILK_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const STEP_EXT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const ZIP_EOCD_SIG_5 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const ZIP_EOCD_SIG_6 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const ZIP_VERSION_MADE_BY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_footprint.zig: const ZIP_VERSION_NEEDED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_model.zig: const MODEL_KEY_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/export_kicad_model.zig: const ROTATION_KEY_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/fab_readiness.zig: const STAGING_BAND_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/fab_readiness.zig: const TOUCH_SLACK_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/font5x7.zig: const DEFAULT_SIZE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/font5x7.zig: const GH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/font5x7.zig: const GLYPHS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/font5x7.zig: const GW is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/font5x7.zig: const TABLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/gerber_verify.zig: const EPS_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/id_insert.zig: const ORDER_ID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/id_insert.zig: const ORDER_IDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/import_fold.zig: const SHARED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/import_fold.zig: const UNCLAIMED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/import_kicad.zig: const SIZES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/import_kicad.zig: const TEST_BOARD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/import_kicad.zig: const UNCONNECTED_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/format.zig: const PROP_CANOPY_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/format.zig: const PROP_REFERENCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/format.zig: const PROP_VALUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/reader.zig: const PROP_CANOPY_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/reader.zig: const PROP_REFERENCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/reader.zig: const PROP_VALUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const DEFAULT_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const DEFAULT_TEXT_SIZE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_AT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_FOOTPRINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_GROUP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_LAYER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_LOCKED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_NET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_PROPERTY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const FORM_VIA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const NM_PER_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const PROP_CANOPY_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const PROP_REFERENCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const PROP_VALUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const TEXT_THICKNESS_RATIO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const UUID_HEX_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const UUID_SEG_A is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const UUID_SEG_B is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const UUID_SEG_C is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const UUID_SEG_D is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/kicad_pcb/writer.zig: const VIA_DEDUP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const ALT_SOURCE_MAX_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const CONVERT_ERROR_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const DEFAULT_SERVE_PORT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const ERROR_READING_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const FILTER_FLAG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/main.zig: const PARSE_PORT_RADIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/drc.zig: const EPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/drc.zig: const SILK_TEXT_PX_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/drc.zig: const STAGING_EXEMPT_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/drc.zig: const VIA_PAD_MAX_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/geometry.zig: const BBOX_MARGIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/geometry.zig: const DEFAULT_RRATIO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/geometry.zig: const MAX_FOOTPRINT_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/geometry.zig: const PATH_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/layout_lint.zig: const DECAP_MAX_LEG_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/layout_lint.zig: const FB_AGGRESSOR_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/layout_lint.zig: const HOT_LOOP_MARGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/module_policy.zig: const BULK_FARADS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AGGRESSOR_KEEPOUT_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ALIGN_CLAMP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ALONG_PACK_SEQ is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AUTOFILL_GND_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AUTOFILL_MARGIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AUTOFILL_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AUTOFILL_SWEEPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const AUTOFILL_VETO_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const BOARD_EDGE_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CAP_W_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CLUSTER_MAX_NET_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CLUSTER_MAX_RAIL_PADS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const COARSE_STRIDE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const COMPACT_EPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_BINS_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_BINS_MIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_BIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_CAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_CLEARANCE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const CONGEST_PITCH_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const DEPRIORITIZE_SCALE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const DISTRIBUTE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ESCAPE_STUB_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const FINAL_CLEAR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const FULLROUTE_DRC_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const FULLROUTE_UNROUTED_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const FULLROUTE_VIA_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const GRID_COURTYARDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const GRID_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const GROUP_FORCE_PER_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const HUB_MASS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const IND_TRACE_W_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const INNER_LAYER_COLORS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const INPUT_LOOP_BOOST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ITERS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const KEEPOUT_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const K_COMPACT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const K_DECOUPLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const K_PROX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const K_SIG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const LAYER_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const LEGALIZE_ITERS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const LOOP_PUL_NH_PER_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const LOOP_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const MAX_DISP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const MAX_WIRE_PTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const MU0_OVER_2PI_NH_PER_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const MULTISTART_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const N_LOOP_VIAS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const PAD_ANCHOR_IC_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const PAD_ANCHOR_PART_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const PASSIVE_MASS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const POLISH_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const PRIORITY_STEP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const PROX_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const RELAX_CONVERGE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const RELOCATE_MAX_CELLS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const RESIDE_W_NBR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const RESIDE_W_RAIL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const RESIDE_W_SIG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROT_CAND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_BLOCK_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_DECLUMP_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_ITERS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_K_ATT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_K_CENTER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_K_REP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_MAX_STEP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_ORIGIN_GRID_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_PACK_LANES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_POLISH_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_POLISH_PASSES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_RESIDE_PASSES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_SEPARATE_ITERS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUGH_STEP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUTED_POLISH_CELLS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUTED_POLISH_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ROUTED_POLISH_SWEEPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const SEED_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const SIGNAL_LAYER_COLORS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const STAGE_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const TIGHTEN_CELLS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const TUCK_GND_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const UNROUTABLE_PENALTY_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const VIA_IND_NH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const VIA_TO_PLANE_DIA_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const W_ALIGN_AUTO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const W_ALIGN_TIDINESS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const W_CONGEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ZONE_FORCE_PER_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/optimizer.zig: const ZONE_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/outline.zig: const CORNER_SEGS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/outline.zig: const L_POLY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pad_shape.zig: const L_POLY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pad_shape.zig: const SIMPLIFY_TOL_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pin_roles.zig: const MAX_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pour.zig: const BLOCKED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pour.zig: const MAX_CELLS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/pour.zig: const UNLABELED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const EMPTY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const ESCAPE_VIA_RINGS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const GATE_RINGS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const INNER_COST_MULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const MAX_NET_PRIORITY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const MAX_NODES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const NETCLASS_BITS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const POUR_COST_MULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const PQ is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const RETURN_PATH_RADIUS_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const RIPUP_CROSS_PEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const RIPUP_MAX_ROUNDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const SQRT2 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const STITCH_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const TOP_FIRST_MAX_PADS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const TOP_FIRST_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/placement/router.zig: const VIA_COST_MULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/png.zig: const SIGNATURE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/query.zig: const DESIGN_BLOCK_MARKER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/query.zig: const MAX_DESIGN_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const LAYOUT_DASH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const MUTED_EM_DASH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const PILL_WARN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const ROUGH_TODO_CELL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const ROW_TD_CLOSE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const ROW_TD_CODE_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const SCHEMATIC_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const SCHEMATIC_VIEWER_JS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const STARRED_TODO_CELL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const STATIC_SVG_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const SUBC_TOGGLE_SCRIPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const TABLE_DETAILS_CLOSE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_html.zig: const TD_CELL_SEP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const BRANCH_BUS_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const BUS_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const CELL_BLOCK_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const DESC_LINE_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const FAR_X_SENTINEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const HUB_VPAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const MIN_VB_HEIGHT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const MIN_VB_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const NOTE_BLOCK_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const PIN_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const SECTION_BORDER_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const SECTION_PAD_X is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const TITLE_LINE_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_json.zig: const VB_MARGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const ACCENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const AW_GND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const AW_PROX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const AW_SIG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const BG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const BLAME_HI is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const BLAME_LO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const BLAME_MID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const COURT_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const DIM_A is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const DRC_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const EDGE_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const GOOD_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const GRID_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const HEADER_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const LEGEND_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const LOOP_RET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const MARGIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const MAX_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const MAX_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const MIN_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const PAD_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const PAD_COL_BOT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const PAD_HOLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const PAD_NPTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const PAD_PTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SHEET_CAP_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SHEET_COLS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SHEET_GAP_PX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SHEET_MAX_TILES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SILK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SILK_BOT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const SS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const TEXT_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const TEXT_DIM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const TRACK_BOT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const TRACK_INNER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const TRACK_TOP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const VIA_COL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_pcb_png.zig: const VIA_HOLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const BRANCH_BUS_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const FAR_X_SENTINEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const HUB_TITLE_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const ICON_OFF_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const LABEL_BASELINE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PASSIVE_HIT_PAD_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PASSIVE_LABEL_OFFSET_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PASSIVE_LABEL_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PASSIVE_VALUE_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PORT_BLOCK_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PORT_LABEL_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const PORT_SPACING is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const RATING_LINE_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const TERMINAL_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const TERMINAL_INSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/branch.zig: const TEXT_G_CLOSE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/connection.zig: const BUS_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/connection.zig: const FAR_X_SENTINEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/connection.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/connection.zig: const NC_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/connection.zig: const PIN_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const AMP_BODY_SIZE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const AMP_HALF_RATIO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const CAP_PLATE_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const CAP_PLATE_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const DEFAULT_SYM_HALF_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_BAR1_HALF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_BAR2_HALF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_BAR2_OFF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_BAR3_HALF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_BAR3_OFF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const GND_STUB_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const ICON_LABEL_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const IND_ARC_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_ARROW_FOOT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_ARROW_HALF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_INNER_LABEL_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_PIN_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const LDO_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const NC_HALF_SIZE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const PASSIVE_BODY_H is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const PASSIVE_BODY_W is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const TR_BODY_SIZE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const TR_GP_OFF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const TR_INSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const TR_OFF_X is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/draw.zig: const TR_PIN_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const HALF_DIVISOR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const HUB_TITLE_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const HUB_VPAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const PIN_LABEL_PAD_X is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const PIN_LABEL_PAD_Y is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const PIN_NUMBER_BASELINE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const PIN_NUMBER_INSET_LEFT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const PIN_NUMBER_INSET_RIGHT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const SVG_BOTTOM_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const SVG_RIGHT_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/render_svg/section_inset.zig: const SVG_TOP_MARGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/req_checks.zig: const CURRENT_TOLERANCE_F is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/req_checks.zig: const DC_EQUIV_RESISTOR_OHMS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/req_checks.zig: const PIN_NOT_FOUND_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/req_checks.zig: const VALUE_TOLERANCE_PF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/review_html.zig: const BODY_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/review_json.zig: const COMPONENT_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/review_json.zig: const NET_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/review_json.zig: const REF_DES_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/review_json.zig: const STATUS_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve.zig: pub const 'Handler' uses a vague name
src/serve/account_page.zig: const ERR_EXPECTED_OBJECT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/account_page.zig: const ERR_INVALID_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/account_page.zig: const ERR_MISSING_BODY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/account_page.zig: const OK_JSON_TRUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const CONTENT_TYPE_ZIP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const EMPTY_DATASHEETS_REQS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const ERR_BUILD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const ERR_NO_BODY_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const ERR_OK_FALSE_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HEADER_CONTENT_DISPOSITION is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HEADER_CONTENT_TYPE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HEADER_CORS_ALLOW_ORIGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const MAX_SOURCE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const NAME_FIELD_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const OK_JSON_TRUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const OK_VERSION_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/api.zig: const STREAM_BUF_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/assets_css.zig: const INDEX_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/assets_css.zig: const NAVBAR_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_AAGUID_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_CRED_DATA_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_CRED_LEN_OFFSET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_FLAGS_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_HEADER_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_RPID_HASH_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const AUTH_DATA_SIGN_COUNT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const B64URL_JS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_ADDITIONAL_MASK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_ADDITIONAL_U16 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_ADDITIONAL_U32 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_ADDITIONAL_U64 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_ADDITIONAL_U8 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_HEADER_LEN_U16 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_HEADER_LEN_U32 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_HEADER_LEN_U64 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_HEADER_LEN_U8 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_MAJOR_MAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CBOR_TAG_BITS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CHALLENGE_COOKIE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const CHALLENGE_TTL_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const ERR_INVALID_CLIENT_DATA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const ERR_INVALID_EMAIL_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const ERR_INVALID_JSON_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const ERR_MISSING_BODY_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const ERR_MISSING_ID_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HEADER_LOCATION is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HEADER_SET_COOKIE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HOST_LOCALHOST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const INVITE_RAND_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const INVITE_TTL_SECONDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const MAX_INVITES_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const MAX_SESSION_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const OK_JSON_TRUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const PAGE_STYLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const PATH_AUTH_LOGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SEC1_TAG_AND_X_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SEC1_UNCOMPRESSED_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SECONDS_PER_DAY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SESSION_COOKIE_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SESSION_RAND_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SESSION_TTL_DAYS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const SESSION_TTL_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const URL_SCHEME_HTTP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const URL_SCHEME_HTTPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/auth.zig: const URL_SCHEME_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/bom_html.zig: const STEP_EXT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const ERR_COMPONENT_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const ERR_COMPONENT_PARSE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const ERR_INVALID_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const FORM_COMPONENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const FORM_COMPONENT_FAMILY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const FORM_REQUIREMENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_info.zig: const MAX_COMPONENT_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const COOKIE_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const DOWNLOAD_TIMEOUT_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const FIELD_MANUFACTURER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const FIELD_PART_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const HEX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const HOST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const MAX_DOWNLOAD_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const MAX_PAGE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const MIN_VARIANT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const OOM_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const PAGE_TIMEOUT_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const PART_ID_MARKER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const PART_VIEW_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const REFERER_HEADER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const SUGGESTION_URL_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/component_search.zig: const USER_AGENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/diag_format.zig: const MAX_SOURCE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const BROWSER_UA is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const DOWNLOAD_TIMEOUT_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const FORM_CT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const FORM_FIELD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const GOTO_MARKER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const JSON_ACCEPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const JSON_CONTENT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const KEYWORD_PATH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const MAX_DOWNLOAD_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const MAX_RESPONSE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const SEARCH_TIMEOUT_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const TOKEN_PATH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/digikey.zig: const TOKEN_TIMEOUT_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const COMPONENT_PATH_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_CANNOT_READ_FILE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_CANNOT_WRITE_FILE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_GENERATED_PART is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_INSTANCE_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_JSON_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_JSON_MISSING_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_JSON_NO_BODY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_MALFORMED_INSTANCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_MISSING_REF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_NO_EDITABLE_VALUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const ERR_REBUILD_FAILED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const HEADER_CORS_ALLOW_ORIGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const IMPORT_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const INSTANCE_HEAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const INSTANCE_OPEN_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_COMPONENT_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_OLD_COMPONENT_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_PINS_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_REF_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_SRC_OFF_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const JSON_VALUE_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const MAX_SOURCE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const OK_JSON_TRUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const PIN_HEAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit.zig: const SECTION_OPEN_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit_assist.zig: const FOOTPRINT_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit_assist.zig: const HEADER_CORS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit_assist.zig: const MAX_LIB_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit_assist.zig: const MAX_SOURCE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/edit_assist.zig: const SEXP_EXT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/editor_page.zig: const EDITOR_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const FAR_AWAY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const JSON_F4 is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const MAX_FOOTPRINT_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const MAX_PCB_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const RECT_FORM_ITEMS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const SEXP_EXT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/footprint_preview.zig: const SVG_BBOX_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/history.zig: const LAYOUTS_FILE_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/history.zig: const LAYOUT_SUBDIR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/history.zig: const MAX_LAYOUT_SNAPSHOTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/history.zig: const NOTE_MAX_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/history.zig: const SEXP_FILE_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/layout_match.zig: const N_SIDES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library.zig: const ERR_INVALID_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library.zig: const FOOTPRINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library.zig: const MAX_LIB_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library.zig: const PIN_FORM_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library.zig: const SEXP_EXT_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const CONTROLS_HTML is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const EMPTY_PADS_TAIL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const MAX_BODY_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const MAX_FOOTPRINT_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const MAX_MODEL_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const PAGE_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/library_3d.zig: const PARAM_FOOTPRINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const HTTP_ACCEPTED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const JSONRPC_ENVELOPE_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const JSONRPC_INVALID_PARAMS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const JSONRPC_INVALID_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const JSONRPC_METHOD_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp.zig: const JSONRPC_SERVER_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const ERR_BUILD_FAILED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const ERR_LINE_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const ERR_NOT_DESIGN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const FUNCTION_FIELD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_COMPONENT_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_COUNT_RESULTS_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_DESCRIPTION_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_ERR_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_MANUFACTURER_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_NET_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_OK_QUERY_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const JSON_VALUE_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const KEY_COMPONENTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const KEY_FOOTPRINTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const KEY_PART_NUMBER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const NAME_FIELD_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const REF_DES_FIELD_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const SEARCH_LIMIT_DEFAULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const SEARCH_LIMIT_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const STOCK_LIMIT_DEFAULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/mcp_tools.zig: const VERSION_SNAPSHOT_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/modules.zig: const COPY_SCRIPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/modules.zig: const ERR_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/modules.zig: const MAX_MODULE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/modules.zig: const PAGE_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ARROW is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const CORS_HEADER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_INVALID_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_MISSING_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_NOT_OBJECT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_NO_BODY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_READ_NOTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_RESOLVE_PATH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ERR_WRITE_NOTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const HTTP_PAYLOAD_TOO_LARGE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const ISO_DATE_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const MAX_NOTES_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const NOTE_ID_HEX_LEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const OK_JSON is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const TASK_DONE_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/notes.zig: const TASK_OPEN_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const ERR_INVALID_GRANT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const ERR_INVALID_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const ERR_MISSING_CLIENT_ID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const ERR_MISSING_REDIRECT_URI is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const HEADER_CORS_ALLOW_ORIGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const HTTP_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const KEY_CLIENT_ID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth.zig: const KEY_REDIRECT_URI is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth_store.zig: const ACCESS_TOKEN_TTL_DAYS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth_store.zig: const ACCESS_TOKEN_TTL_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/oauth_store.zig: const AUTH_CODE_TTL_SECS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_describe.zig: const LONG_LOOP_FLOOR_NH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_describe.zig: const ORIGIN_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_describe.zig: const REF_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const ALIGNBAR_HTML is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const AUTO_EXT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const BAD_JSON_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const BAR_GRP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const BAR_GRP_END is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const BAR_SEP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const CHECKED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const CLEARANCE_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const COURTYARD_MODAL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const CT_HDR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const DIV2_END is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const EMBED_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const FAB_MODAL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const KIND_AUTO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const KIND_MANUAL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const LAYOUTS_EXT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MARGIN_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MAX_AUTO_LAYOUTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MAX_FP_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MCP_ERR_MISSING_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MCP_ERR_NO_DESIGN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const MCP_OK_LAYOUT_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const NAME_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const NET_BROWN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const NET_WHITE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const NO_BLOCK_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const NO_SUB_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const OK_JSON_TRUE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const ORIGIN_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const OUTLINE_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PAGE_CSS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PARTS_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PCB_3D_STAGE_HTML is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PCB_3D_TOGGLE_JS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PIN_KEY_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PLACEMENT_ERR_MSG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PNG_DEFAULT_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const PT_PAIR_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const REF_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const ROUTED_VIA_PREVIEW_MAX_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const SCALE_MAX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const SCALE_MIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const STATUSBAR_HTML is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TARGET_PX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TEXTS_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TIP_DRAW is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TIP_OUTLINE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TIP_POLY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TIP_RULER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TIP_TEXT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TOOLSTRIP_HTML is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const TRACK_JSON_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const VIAS_ARR_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const VIA_JSON_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const VIA_MATCH_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/pcb_layout_page.zig: const VIEW_CHIP_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/schematic_viewer_js.zig: const SCHEMATIC_VIEWER_JS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/static_assets.zig: const REGISTRY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const GAP_BANDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const LAMBDA_DEFAULT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const N_BANDS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const N_SIDES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const W_EDGE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/style_score.zig: const W_GAP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BACKUP_DIR_NAME is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BACKUP_INFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BLOCK_STAGE_GAP_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BLOCK_STAGE_ORIGIN_X_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BLOCK_STAGE_ORIGIN_Y_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const BOARD_ITEM_OP_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const COURTYARD_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const DEFAULT_ROUNDRECT_RRATIO is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const ERR_BUILD_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const ERR_NOT_A_DESIGN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const ERR_NO_PCB_PATH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const ERR_PCB_PARSE_FAILED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const ERR_PCB_READ_FAILED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FAB_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FIELD_CANOPY_NET is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FIELD_CANOPY_SECTION is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FIELD_CANOPY_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FORM_HEAD_AT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FORM_HEAD_FOOTPRINT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FORM_HEAD_LAYER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FORM_HEAD_PROPERTY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const FORM_HEAD_UUID is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const HEADER_CORS_ALLOW_ORIGIN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const HTTP_CONFLICT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const HTTP_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MAX_BOARD_BACKUPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MAX_FOOTPRINT_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MAX_PCB_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MAX_PLACEMENT_VIOLATIONS_LISTED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MAX_SOURCE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const MODEL_DRIFT_EPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const NM_PER_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const OP_CREATE_BOARD_ITEM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const OP_SET_FIELD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PATH_FMT_FP_SEXP is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PLACEMENT_EPS_DEG is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PLACEMENT_EPS_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const POLY_MIN_POINTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_ANY_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_LAYER_DWGS_USER is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_LAYER_F_CRTYD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_LAYER_F_FAB is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_LAYER_F_SILK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_PADSTACK_NORMAL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_PADTYPE_NPTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_PADTYPE_PTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_PADTYPE_SMD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_TYPE_URL_BOARDSHAPE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_TYPE_URL_BOARDTEXT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const PROTO_TYPE_URL_PAD is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const READONLY_MODE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const RECT_NODE_MIN_CHILDREN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SEED_BOX_LABEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SILK_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SOURCE_DIR_SUFFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SOURCE_HEADER_FMT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_BOX_GAP_X_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_BOX_GAP_Y_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_BOX_PAD_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_BOX_STROKE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_HALF is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_LABEL_H_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_LABEL_SIZE_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_ORIGIN_X_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_ORIGIN_Y_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_PART_PITCH_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const STAGE_SECTIONS_PER_ROW is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SWAP_REASON_GEOMETRY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SWAP_REASON_MODEL is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/sync.zig: const SWAP_REASON_REFRESH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/account.zig: const AUTH_SCRIPT_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/account.zig: const AUTH_STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/account.zig: const ROLE_OPTIONS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/auth.zig: const B64URL_SCRIPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/auth.zig: const INPUT_DARK_STYLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/auth.zig: const INPUT_MID_STYLE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/auth.zig: const STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/library.zig: const STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/library.zig: const UPLOAD_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/mcp_docs.zig: const SCRIPT_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/mcp_docs.zig: const STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/oauth.zig: const STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const DAYS_PER_MONTH_APPROX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const HOME_FILTERS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const HOME_NEW_SCRIPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const HOME_SEARCH_SCRIPT is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const HOME_STYLE_BLOCK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const MAX_CHIPS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const SCHEMATIC_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const SECONDS_PER_DAY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const SECONDS_PER_HOUR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/templates/pages.zig: const SECONDS_PER_MINUTE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const MAX_KICAD_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const MAX_STEP_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const SEXP_PATH_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const TMP_DIR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const TMP_EXTRACT_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload.zig: const TMP_ZIP_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const BOUNDARY_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const FILENAME_PREFIX is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const HTTP_BAD_REQUEST is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const HTTP_INTERNAL_ERROR is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const SEXP_PATH_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/upload_package.zig: const UPLOAD_LOG_TEMPLATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const BINARY_EXTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const DEFAULT_LIST_ENTRIES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const DENY_BASENAMES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const DENY_PREFIXES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const DENY_SUFFIXES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const ERR_ACCESS_DENIED is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const ERR_FILE_TOO_LARGE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const ERR_NOT_FOUND is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const ERR_REFUSE_SYMLINK is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const JSON_LIBRARY_CHANGES_KEY is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const JSON_PATH_OPEN is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_COMPONENTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_DATASHEETS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_FOOTPRINTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_MODELS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_MODULES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_PARTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_PINOUTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const LIB_SOURCES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const MAX_FILE_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const MAX_GLOB_RESULTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const MAX_PATH_BYTES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const READ_ONLY_EXTS is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const READ_PREFIXES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/serve/vfs.zig: const WRITE_PREFIXES is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/parser.zig: const MAX_PARSE_DEPTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/parser.zig: const MIL_TO_MM is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/printer.zig: const FALLBACK_FLOAT_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/printer.zig: const FALLBACK_INT_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/printer.zig: const FALLBACK_UNIT_VAL_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/printer.zig: const NODE_EQ_FLOAT_TOLERANCE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/sexpr/printer.zig: const SHORT_LIST_MAX_WIDTH is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
src/zipfile.zig: const DOS_DATE is SCREAMING_SNAKE (Zig consts are snake_case, or PascalCase for a type)
