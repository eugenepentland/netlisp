# guardian-snapshot v1
src/import_kicad.zig: string literal {s}/lib/components/{s}.sexp appears 3 times — extract a const
src/serve/bom_html.zig: string literal manufacturer appears 3 times — extract a const
src/serve/edit.zig: string literal {{\"ok\":true,\"version\":{d}}} appears 3 times — extract a const
src/serve/mcp_tools.zig: string literal component appears 4 times — extract a const
src/serve/templates/account.zig: string literal </button> appears 6 times — extract a const
src/serve/templates/account.zig: string literal </label> appears 3 times — extract a const
src/serve/templates/account.zig: string literal </option> appears 4 times — extract a const
src/serve/templates/account.zig: string literal <div class=\"card\"> appears 4 times — extract a const
src/serve/templates/account.zig: string literal <p class=\"muted\"> appears 5 times — extract a const
src/serve/templates/auth.zig: string literal  id=\"email\" appears 3 times — extract a const
src/serve/templates/auth.zig: string literal  placeholder=\"Email address\" appears 3 times — extract a const
src/serve/templates/auth.zig: string literal  required appears 3 times — extract a const
src/serve/templates/auth.zig: string literal  type=\"email\" appears 3 times — extract a const
src/serve/templates/auth.zig: string literal <!DOCTYPE html> appears 4 times — extract a const
src/serve/templates/auth.zig: string literal </button> appears 3 times — extract a const
src/serve/templates/auth.zig: string literal </script> appears 4 times — extract a const
src/serve/templates/auth.zig: string literal </title> appears 4 times — extract a const
src/serve/templates/auth.zig: string literal <div class=\"auth-card\"> appears 4 times — extract a const
src/serve/templates/auth.zig: string literal <div class=\"status\" id=\"status\"> appears 3 times — extract a const
src/serve/templates/auth.zig: string literal <meta name=\"viewport\" content=\"width=device-width,initial-scale=1\"> appears 4 times — extract a const
src/serve/templates/auth.zig: string literal <span class=\"brand\"> appears 4 times — extract a const
src/serve/templates/library.zig: string literal  href=\" appears 3 times — extract a const
src/serve/templates/library.zig: string literal </button> appears 3 times — extract a const
src/serve/templates/oauth.zig: string literal  type=\"hidden\" appears 5 times — extract a const
src/serve/templates/oauth.zig: string literal </strong> appears 3 times — extract a const
src/serve/templates/oauth.zig: string literal <strong> appears 3 times — extract a const
src/serve/templates/pages.zig: string literal  class=\"design-card-link\" appears 3 times — extract a const
src/serve/templates/pages.zig: string literal  href=\" appears 4 times — extract a const
src/serve/templates/pages.zig: string literal <div class=\"design-card-title\"> appears 3 times — extract a const
src/serve/templates/pages.zig: string literal <span class=\"section-chip-more\"> appears 3 times — extract a const
src/serve/templates/pages.zig: string literal <span class=\"sep\"> appears 3 times — extract a const
