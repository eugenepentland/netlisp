# guardian-snapshot v1
note: a missing flush() truncates output in 0.15 — add w.interface.flush() before returning.
src/commands.zig:141: buffers stdout/stderr with no reachable flush() before returning
